{
  "name": "advances-ai-trip-planer",
  "version": "1.0.0",
  "description": "Root of the AI Trip Planner monorepo",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix Backend\" \"npm run dev --prefix frontend\"",
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && mv frontend/dist ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@ai-sdk/google": "^3.0.37",
    "@assistant-ui/react-ai-sdk": "^1.3.11",
    "@google/generative-ai": "^0.24.1",
    "ai": "^6.0.111",
    "axios": "^1.13.5",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-validator": "^7.3.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.2.2",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
